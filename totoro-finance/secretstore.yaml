apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: totoro-finance-secret-store
  namespace: totoro-finance
spec:
  provider:
    gcpsm:
      auth:
        secretRef:
          secretAccessKeySecretRef:
            name: gcp-credentials-totoro-finance
            key: key.json
      projectID: totoro-finance

apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: totoro-media-secret-store
  namespace: totoro-media
spec:
  provider:
    gcpsm:
      auth:
        secretRef:
          secretAccessKeySecretRef:
            name: gcp-credentials-totoro-media
            key: key.json
      projectID: totoro-media

